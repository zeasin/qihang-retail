<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.qihangerp.mapper.ErpLogisticsCompanyMapper">

    <resultMap id="BaseResultMap" type="cn.qihangerp.model.entity.ErpLogisticsCompany">
            <id property="id" column="id" jdbcType="BIGINT"/>
            <result property="platformId" column="platform_id" jdbcType="INTEGER"/>
            <result property="shopId" column="shop_id" jdbcType="INTEGER"/>
            <result property="logisticsId" column="logistics_id" jdbcType="BIGINT"/>
            <result property="code" column="code" jdbcType="VARCHAR"/>
            <result property="name" column="name" jdbcType="VARCHAR"/>
            <result property="remark" column="remark" jdbcType="VARCHAR"/>
            <result property="type" column="type" jdbcType="VARCHAR"/>
            <result property="supplierId" column="supplier_id" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,platform_id,shop_id,supplier_id,
        logistics_id,code,name,type,
        remark
    </sql>
    <select id="selectDistinctByName" resultMap="BaseResultMap">
        SELECT * FROM (
            SELECT 
                *,
                ROW_NUMBER() OVER (PARTITION BY name ORDER BY platform_id) as rn
            FROM erp_logistics_company
            WHERE name LIKE CONCAT('%', #{name}, '%')
              AND status = 0
        ) t
        WHERE rn = 1
        ORDER BY name
    </select>
</mapper>
